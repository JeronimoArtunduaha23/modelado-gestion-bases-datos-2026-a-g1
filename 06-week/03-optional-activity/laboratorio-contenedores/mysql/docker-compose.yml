services:
  mysql:
    image: mysql:8
    container_name: corhuila-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: mysql123
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./sql:/docker-entrypoint-initdb.d

volumes:
  mysql_data: